# Intentionally vulnerable docker-compose for supSec demo
services:
  app:
    image: myapp:latest
    privileged: true
    network_mode: host
    environment:
      DB_PASSWORD: "SuperSecret123!"
      API_KEY: "sk-proj-abcdefghij1234567890"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    cap_add:
      - SYS_ADMIN
